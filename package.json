{
  "name": "garnished",
  "version": "1.0.0",
  "description": "Full stack web app for Garnished LLC",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run devclient\" \"npm run devserver\"",
    "devclient": "cd client && npm run dev",
    "devserver": "cd server && npm run dev",
    "pubclient": "git subtree push --prefix client/dist origin dist",
    "pubclientpreview": "git subtree push --prefix client/dist test dist",
    "buildclientpreview": "cd client && npm run buildpreview",
    "pubserver": "cd server && fly deploy"
  },
  "author": "Bryan Miller (bryan@bmilcs.com)",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
